# Requirements for EKPC load forecasting app
# Pin minimal compatible versions where reasonable; adjust if you have stricter needs.
# TensorFlow includes Keras. If you need GPU support install the appropriate tensorflow package for your system.

tensorflow>=2.12.0
pandas>=1.5.0
numpy>=1.24.0
streamlit>=1.22.0
h5py>=3.8.0


matplotlib>=3.7.0
scikit-learn>=1.2.0
